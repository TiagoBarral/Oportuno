#!/bin/sh
# Pre-commit: type check only. Tests run on pre-push.

cd app || exit 1

echo "Running type check..."
bunx tsc --noEmit
if [ $? -ne 0 ]; then
  echo ""
  echo "Commit blocked: TypeScript errors found. Fix type errors before committing."
  exit 1
fi

echo "Type check passed."
exit 0
